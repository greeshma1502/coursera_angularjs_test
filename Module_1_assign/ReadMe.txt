Module_1_assign contains "app.js" and "index.html"

html file contains 
- an input contains which takes in lunch items,
- a button "check if too much", on clicking triggers method "checkIfTooMuch()", which evaluates whether number of
  lunch items is too much or not.
- an output, which displays the desired message on evaluating the method.

